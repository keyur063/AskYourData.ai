# AskYourData.ai — Backend dependencies (Lean MVP)
# Install with: pip install -r requirements.txt
# (or, if using a venv: python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt)

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20        # required by FastAPI for multipart/form-data (file uploads)

# --- Supabase ---
supabase==2.11.0                # Python client for Supabase (auth verification, storage, DB)

# --- LLM ---
openai==1.59.6                  # also used for Groq — its API is OpenAI-schema-compatible,
                                 # point the client at GROQ_BASE_URL instead of OpenAI's endpoint

# --- Data / query execution ---
duckdb==1.1.3
pandas==2.2.3                   # used for CSV parsing/schema inference in ingestion, not as an execution backend in lean scope

# --- Validation ---
pydantic==2.10.4
pydantic-settings==2.7.1        # for config.py env-driven settings
jsonschema==4.23.0               # validates LLM planner output against schemas/query-ir.schema.json

# --- Utilities ---
python-dotenv==1.0.1              # loads .env in local dev
httpx==0.28.1                      # async HTTP client (used by supabase-py and available for adapters)
cryptography==44.0.0                # Fernet symmetric encryption for per-user stored Groq API keys

# --- Dev / testing (not needed in production image, but keep in this file
#     for lean scope simplicity; split into requirements-dev.txt later if
#     the project grows) ---
pytest==8.3.4
pytest-asyncio==0.25.2
