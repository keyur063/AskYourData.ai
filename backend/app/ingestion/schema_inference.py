"""
Schema inference — column types, nullability, sample values from a DataFrame.

Implemented in ticket L2.2 (Session 2).
Writes results to catalog_tables / catalog_columns via catalog.service.
"""
# TODO L2.2: implement infer_schema(df, workspace_id, file_id) -> list[CatalogColumn]
