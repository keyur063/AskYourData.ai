"""
Supabase Storage helpers — upload/fetch raw CSV files.

Implemented in ticket L2.1 (Session 2).
Bucket: 'files' (private, created in Step 1).
Storage path convention: {workspace_id}/{content_hash}/{filename}
"""
# TODO L2.1: implement upload_file(workspace_id, content_hash, filename, data) -> str
# TODO L2.1: implement fetch_file(storage_path) -> bytes
