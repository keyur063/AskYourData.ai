"""
CSV file validation — type checks and size cap.

Implemented in ticket L2.1 (Session 2).
Max file size: MAX_FILE_SIZE_MB from settings (default 5 MB).
"""
# TODO L2.1: implement validate_csv_upload(file, settings)
